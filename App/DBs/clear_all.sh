#!/usr/bin/env bash
rm groups.json && touch groups.json
rm courses.json && touch courses.json
rm employees.json && touch employees.json
rm points.json && touch points.json
rm work_flags.json && touch work_flags.json
